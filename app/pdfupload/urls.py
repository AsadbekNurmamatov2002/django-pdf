from django.urls import path
# from .views import Home, render_pdf_view
app_name="pdfupload"
urlpatterns = [
    # path('<str:id>/', Home, name='home'),
    # path('pdf/<str:pk>/',render_pdf_view ,name="pdffile")
]
